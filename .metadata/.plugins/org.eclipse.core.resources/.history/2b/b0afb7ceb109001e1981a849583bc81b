@charset "UTF-8";

#myBoardSection {
    margin-top: 80px;
    margin-bottom: 50px;
    margin-left: 200px;
    display: flex;
    /* justify-content(가로 가운데)랑 align-items(세로 가운데) 쓰기 위해 선언!
        ≫ column을 하는 순간 반대로 다 바뀜 */
    align-items: center;
    flex-direction: column; /* 세로 방향, row는 가로 방향 */
}

#myBoardTitle {
    font-size: 30px;
    border-bottom: 2px solid #40513B;
    margin-top: 30px;
    margin-bottom: 40px;
}

.actionArea {
    margin-top: 30px;
    width: 750px;
}

.searchArea {
    float: right;
}

.searchInput {
    float: right;
    width: 160px;
    height: 30px;
    padding: 10px 12px;
    padding-right: 20px;
    border-radius: 0.4rem;
    background-color: #f3f3f3;
    border: 1px solid #b4ae88;
}

.searchIcon {
    width: 15px;
}

.searchIconATag {
    position: relative;
    left: 155px;
    top: 6px;
}

/* 카테고리 드롭다운 */

.dropdown{
    float: right;
    margin-right: -10px;
}

#dropbtn{
    border: 1px solid #b4ae88;
    border-radius: 8px;
    background-color: #f3f3f3;
    color: gray;
    width :80px;
    height: 30px;
    text-align: center;
    cursor : pointer;
    font-size : 13px;
}

.dropdown-content{
    display : none;
    position : absolute;
    z-index : 9999;
    font-weight: 400;
    min-width : 80px;
}

.dropdown-content span{
    display: flex;
    align-items: center;
    justify-content: center;   /* 가로 세로 가운데 정렬하려면 4줄 추가 */
    flex-direction: column;
    color : rgb(37, 37, 37);
    font-size: 13px;
}

.dropdown-content span:first-child {
    border-radius: 8px 8px 0 0;
}

.dropdown-content span:last-child {
    border-radius: 0 0 8px 8px;
}

.dropdown-content span:hover{
    cursor: pointer;
    background-color : #f7df59;
    color:white;
}

.dropdown:hover .dropdown-content {
    display: block;
}

.selectCate{
    width: 80px;
    height: 35px;
    background-color: #f9efb7;
}

.myBoardTitle_wrap {
    display: -webkit-box;
    -webkit-box-orient: vertical;
    -webkit-line-clamp: 1; /* 표시할 줄 수 */
    overflow: hidden;
    text-overflow: ellipsis;
}

.myBoardTitle {
    word-break: break-all;
    text-align: center;
}

/* 테이블 */

.myBoardTable {
    width: 750px;
    margin-top: 10px;
    padding: 10px;
    text-align: center;
    border-width: 1px;
    border-style: solid;
    border-color: black;
    border-collapse: separate;
    border-spacing: 0;
    font-size: 15px;
    border-radius: 10px;
}

th, td {
    padding: 10px;
}

.myBoardTable thead tr th {
    border-bottom: 1px dotted black;
    background-color: #f8f3d5;
}

.myBoardTable thead tr th:nth-child(1) {
    border-radius: 10px 0px 0px 0px;
}

.myBoardTable thead tr th:last-child {
    border-radius: 0px 10px 0px 0px;
}

.myBoardTable > tbody > tr:hover{
    background-color: #f8ea9b;
    cursor: pointer;
}

.myBoardTable > tbody td:first-child {
    border-radius: 8px 0px 0px 8px;
}

.myBoardTable > tbody td:last-child {
    border-radius: 0px 8px 8px 0px;
}

/* 페이지 */

.page {
    margin-top: 35px;
    display: flex;
    flex-direction: row;
}

.pagination > li {
    list-style-type: none;
    float: left;
}

.pagination > li > .num {
    margin: 10px;
    width: 20px;
    height: 20px;
    text-decoration-line: none;
    color: black;
    font-size: 16px;
    display: inline-block;
    text-align: center
}

.pagination > li > .num:hover {
    color: white;
    background-color: #f2c866;
    border-radius: 100%;
}

.arrowKey {
    margin-top: 12px;
    margin-left: 10px;
    margin-right: 10px;
    block-size: 14px;
    display: block;
}
